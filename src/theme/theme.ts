import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Um azul para a cor principal
    },
    secondary: {
      main: '#dc004e', // Um rosa/vermelho para detalhes
    },
  },
});

export default theme;